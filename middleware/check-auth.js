import TokenStorage from '@/utils/token-storage';

export default function ({ store, redirect, route, app }) {
    // let isAuth = !!TokenStorage.getAccessToken()
    //
    // if (!isAuth && route.path !== '/auth' && route.path.indexOf('/streams-online') === -1) {
    //     return redirect('/auth')
    // }
    //
    // if (isAuth && route.path === '/auth') {
    //     return redirect('/')
    // }
    // TODO uncomment
}